package src.main.java.behavioral.Iterator;

/**
 * 
 * An interface which will contain the method for fetching the instance of
 * iterator of the structure implement Container interface
 * 
 */
public interface Container {
	/**
	 * This method is used for fetching the instance of the iterator
	 * 
	 * @return Iterator instance
	 */
	Iterator getIterator();
}
